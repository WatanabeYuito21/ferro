[package]
name = "ferro-core"
version.workspace = true
edition.workspace = true

[dependencies]
native-tls = "0.2.18"
rusqlite = { version = "0.40.2", features = ["bundled"] }
thiserror = "2.0.20"

[dev-dependencies]
tempfile = "3.27.0"
